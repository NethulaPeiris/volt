---
deployment:
  tasks:
    - name: Install dependencies
      command: npm install

    - name: Build the React app
      command: npm run build

    - name: Deploy to public_html/test.acicts.lk
      command: rsync -av --delete build/ ~/public_html/test.acicts.lk/
